<div class="row">
	<div class="col-lg-12 col-md-12 col-sm-12">
		<div class="container">
			<h2 align="center"><b>PROCESSOS</b></h2><br>
			<p><%= link_to 'Novo Processo', new_procedure_path, class: 'btn btn-success' %></p>
			<table class="table table-bordered">
				<thead>
					<th>Paciente</th>
					<th>Drº/Profissional</th>
					<th>Data</th>
				</thead>
				<tbody>
					<% @procedures.each do |procedure| %>
					<tr>
						<td><%= procedure.patient %></td>
						<td><%= procedure.doctor %></td>
						<td><%= datebr(procedure.date_procedure) %></td>
						<td><%= link_to :Exibir, procedure_path(procedure), class: 'btn btn-primary' %></td>
					</tr>
					<% end %>
				</tbody>
			</table>
		</div>
	</div>
</div>